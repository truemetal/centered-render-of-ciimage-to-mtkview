# centered-render-of-ciimage-to-mtkview

You need a metal-capable iOS device to run this project.

I'd expect the rendered image to be centered in the drawable, instead it is rendered at the texture's origin.
